**考前复习中...开个页面记录一下当做题区时积累的方法...还有三天...**`2025/11/13 21:28`

--- 

**升阶构造**

**带参数的矩阵**

**借助待定系数法**

---

**平面相关**

设有三张平面两两相交，且它们的方程分别为

\\[ a_{i1}x+a_{i2}y+a_{i3}z=d_{i1} \\]

---

**含n的行列式优先运用分拆+拉普拉斯**

**找递推！！！**

**利用转置矩阵解方程把行列式解出来(需要矩阵和转置矩阵至少看上去不太一样)**

`唉怎么一套一套的，完全反应不过来啊`

**被难题干傻了...**

*题干*

$$
\begin{vmatrix}
a_{0} & a_{1} & \cdots & a_{n} \\
a_{1} & a_{2} & \cdots & a_{0} \\
\vdots & \vdots & & \vdots \\
a_{n} & a_{0} & \cdots & a_{n-1} 
\end{vmatrix}
$$

*答案*
                       
令$f(x)=\sum_{k=0}^{n}a_{k}x^k$,记$\varepsilon=e^{\frac{2\pi i}{n+1}}$为1的$n+1$次单位根，注意到:
<small>提示，$e^{\pi i}=-1$</small>

$$    
\begin{bmatrix}
a_{0} & a_{1} & \cdots & a_{n} \\
a_{1} & a_{2} & \cdots & a_{0} \\
\vdots & \vdots & & \vdots \\
a_{n} & a_{0} & \cdots & a_{n-1} 
\end{bmatrix}
\begin{bmatrix}
1 & 1 & \cdots & 1 \\
1 & \varepsilon & \cdots & \varepsilon^{n} \\
\vdots & \vdots & & \vdots \\
1^{n} & \varepsilon^{n} & \cdots & \varepsilon^{n^{2}} 
\end{bmatrix}
=
\begin{bmatrix}
f(1) & f(\varepsilon) & \cdots & f(\varepsilon^{n}) \\
f(1) & \varepsilon^{-1}f(\varepsilon) & \cdots & \varepsilon^{-n}f(\varepsilon^{n})\\
\vdots & \vdots & & \vdots \\
f(1) & \varepsilon^{-n}f(\varepsilon) & \cdots & \varepsilon^{-n^{2}}f(\varepsilon^{n})
\end{bmatrix}
$$

*行数与列数错开越来越大，使得需要通过提出 $\varepsilon^{-n}$ 使得含$a_{n}$的多项式得以符合 $f(\varepsilon)$ 的形式*

*在这一过程中，通过 $\varepsilon$  $n+1$ 次单位根的设置，使一些在提出 $\varepsilon^{-n}$ 后的项(比如 $a_{0}$) 的系数得以变回其原本的系数*

*接着，两边取行列式*


$$
\begin{vmatrix}
a_{0} & a_{1} & \cdots & a_{n} \\
a_{1} & a_{2} & \cdots & a_{0} \\
\vdots & \vdots & & \vdots \\
a_{n} & a_{0} & \cdots & a_{n-1} 
\end{vmatrix}
\times
D(1,\varepsilon,\varepsilon^{2},\cdots,\varepsilon^{n})
=
D(1,\varepsilon^{-1},\varepsilon^{-2},\cdots,\varepsilon^{n})
\prod_{k=0}^{n}f(\varepsilon^{k})
$$
